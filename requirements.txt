# Web framework
fastapi==0.115.9
uvicorn[standard]==0.44.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Config & utils
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.2.1
python-dotenv==1.0.1
httpx==0.28.1
aiofiles==23.2.1

# PDF generation
jinja2==3.1.4
weasyprint==62.3

# Excel export
openpyxl==3.1.5

# LangChain — versions exactes du venv de dev.
# NB: `langchain` (méta) et `langchain-community` ne sont PAS utilisés par le code
# et provoquaient des conflits de résolution → volontairement retirés.
langchain-core==1.3.0
langchain-text-splitters==1.1.2

# LLM — Groq (chatbot)
langchain-groq==1.1.2
groq==0.37.1

# Embeddings — HuggingFace (cloud, gratuit) ou Ollama (local, optionnel)
langchain-huggingface==1.2.2
langchain-ollama==1.1.0

# Vector store (versions alignées sur l'environnement de dev qui fonctionne)
langchain-chroma==1.1.0
chromadb==1.5.8
pypdf==6.10.2
python-docx==1.2.0
